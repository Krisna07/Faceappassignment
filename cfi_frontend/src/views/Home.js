import React from "react";

import VideoInput from "./VideoInput";

const Home = () => {
  return (
    <div>
      <h1>Camera</h1>
      <VideoInput />
      <button>Notify</button>
    </div>
  );
};

export default Home;
